# Database Configuration
# For production (Railway automatically sets this)
DATABASE_URL=postgresql://username:password@hostname:port/database

# Node Environment
NODE_ENV=production

# Server Port (Railway automatically sets this)
PORT=3000